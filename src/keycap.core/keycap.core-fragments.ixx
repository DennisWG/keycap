module;

export module keycap.core : fragments;

import : types;

namespace keycap::fragment
{
    enum : u64
    {
        algorithm,
        concenpts,
        error,
        math,
        scopeguard,
        string,
        types,
    };
}